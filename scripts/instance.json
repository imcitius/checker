{
  "name": env.INSTANCE_NAME,
  "project_name": env.PROJECT_NAME,
  "auto_deploy": true,
  "overwrite": true,
  "datacenters": [
    env.INSTANCE_DC
  ],
  "deploy_repository_url": env.DEPLOY_REPO_URL,
  "params": {
    "werf_image": env.WERF_IMAGE,
    "consul_path": env.CONSUL_PATH,
    "version": env.VERSION
  }
}
