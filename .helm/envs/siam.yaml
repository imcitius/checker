db:
  check: false
  password_path: secret/siam/pgsql/checker/pass

app:
  config:
    routes:
      - Host(`web.siam.siamoption.com`) && PathPrefix(`/checker/`)
    ingress:
      cert: wildcard-cert

checker_config_template: |
  {{ .Files.Get configs/siam-config.yaml }}
