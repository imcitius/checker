db:
  check: true
  password_path: secret/mg-prod/pgsql-ext/checker/pass

app:
  config:
    routes:
      - Host(`mediaget.com`) && PathPrefix(`/checker/`)
    ingress:
      cert: wildcard-cert

checker_config_template: |
  {{ .Files.Get configs/mg-prod-config.yaml }}