db:
  check: true
  password_path: secret/mg-staging/pgsql-ext/checker/pass

app:
  config:
    routes:
      - Host(`test.mediaget.com`) && PathPrefix(`/checker/`)
    ingress:
      cert: wildcard-cert

checker_config_template: |
  {{ .Files.Get configs/mg-prod-config.yaml }}
