.build_inject_cmd: &build_inject_cmd |
  go build -ldflags \
  "-X my/checker/config.Version=${CI_COMMIT_REF_NAME}-fly -X my/checker/config.VersionSHA=${CI_COMMIT_SHORT_SHA}" \
  -o ${BINARY_NAME}

fly-deploy:
  stage: deploy
  image: harbor-ks.jgit.me/dockerhub_proxy/library/golang:1.16
  before_script:
    - wget https://releases.hashicorp.com/vault/1.8.5/vault_1.8.5_linux_amd64.zip
    - unzip vault_1.8.5_linux_amd64.zip
    - apt-get update -qq && apt-get install -y curl
    - curl -L https://fly.io/install.sh | sh
  script:
    - export FLY_API_TOKEN=$(./vault read -field=token secret/fly.io/${FLY_ACCOUNT}/pass)
    - *build_inject_cmd
    - /root/.fly/bin/flyctl deploy
  extends:
    - .jgit_filter
    - .docker_europe_tags
  environment:
    name: fly-prod
  needs: []
